export enum RoutePaths {
  root = "/",
  signUp = "/sign_up",
  signIn = "/sign_in",
  companies = "/companies",
  companyDetail = "/company-detail",
  profile = "/profile",
  // only for admins
  userList = "/user-list",
  companyList = "/company-list",
}
