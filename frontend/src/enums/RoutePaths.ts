export enum RoutePaths {
  root = "/",
  signUp = "/sign_up",
  signIn = "/sign_in",
  companies = "/companies",
  companyDetail = "/company-detail",
  profile = "/profile",
  userList = "/user-list",
  companyList = "/company-list",
}
